class Drag_and_drop_mobile {
    constructor(drag, drop) {
        this.drag = drag;
        this.drop = drop;
    }

    show_attributes() {
        console.log('drag: ' + drag + ' drop: ' + drop);
    }
}
